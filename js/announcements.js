function announcementCard(a){return `<article class="card announcement-card ${a.important?'important':''}"><div class="announcement-head"><span class="badge">${escapeHTML(a.category)}</span>${a.important?'<span class="badge badge-danger">Important</span>':''}</div><h3>${escapeHTML(a.title)}</h3><p>${escapeHTML(a.description)}</p><div class="muted" style="font-size:.78rem">${formatDate(a.date)}</div></article>`}
function renderAnnouncements(){const root=document.getElementById('announcement-grid');if(!root)return;const list=getData(CC_KEYS.announcements,[]),search=document.getElementById('announcement-search').value.toLowerCase(),cat=document.getElementById('announcement-category').value;const filtered=list.filter(a=>(!search||`${a.title} ${a.description}`.toLowerCase().includes(search))&&(!cat||a.category===cat));root.innerHTML=filtered.length?filtered.map(announcementCard).join(''):emptyState('No announcements found','Try another search or category.')}
document.addEventListener('DOMContentLoaded',()=>{document.getElementById('announcement-search')?.addEventListener('input',renderAnnouncements);document.getElementById('announcement-category')?.addEventListener('change',renderAnnouncements);renderAnnouncements()});
